return function(p)

  return {

    bg = p.bg,
    fg = p.fg,

    cursorline = p.black,
    selection = p.green,

    comment = p.green,

    keyword = p.green,
    function_name = p.green,
    string = p.green,
    number = p.green,
    type = p.green,

    error = p.green,
    warning = p.green,
    hint = p.green,

    accent = p.green
  }

end
